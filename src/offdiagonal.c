/*
Name: 
*/

#include <stdio.h>
#include <stdlib.h>

int main()
{
    // Create hamiltonian for c = 0 => ham_0
    // Diagonalize it and get eigvecs => eigvecs_0
    // Within each energy subspace, the eigenvectors
    // are mixed. Separate them into up spin and
    // down spin states => eigvecs_0_sep
    // Create hamiltonian for c = finite => ham_c
    // Change basis of ham_c to eigvecs_0_sep
}

